import { neueMontrealRegular } from './fonts'

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${neueMontrealRegular.variable}`}>
      {/* ... rest of your layout */}
    </html>
  )
} 